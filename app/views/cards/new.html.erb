<h1>Make a new card</h1>

<h1>Hello, <%="#{current_user.id}"%></h1>
<%= form_for :card, url: cards_path do |f| %>
<%= f.collection_select :subject_id, Subject.all, :id, :name %><br>

    <%= f.label :front %><br>
    <%= f.text_area :front %>
  </p>

  <p>
    <%= f.label :back %><br>
    <%= f.text_area :back %>
  </p>

  <p>
    <%= f.submit %>
  </p>
<% end %>
